@model IBCustomerSite.ViewModels.DepositViewModel

@{
    ViewData["Title"] = "Withdrawal";
}
<h1>Withdrawal</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Withdrawal">
            <div class="form-group">
                <label asp-for="AccountNumber" class="control-label">Select Account</label>
                <select name="AccountNumber">
                    @foreach (var item in Model.Accounts)
                    {
        <option asp-for="AccountNumber" value="@item.AccountNumber">@Html.DisplayFor(modelItem => item.AccountNumber)</option>}

                </select>

                <span asp-validation-for="Accounts" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* ReSharper disable once UnknownCssClass *@
                <label asp-for="Amount" class="control-label"></label>
                <input asp-for="Amount" class="form-control" />
                <span asp-validation-for="Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* ReSharper disable once UnknownCssClass *@
                <label asp-for="Comment" class="control-label"></label>
                <input asp-for="Comment" class="form-control" />
                <span asp-validation-for="Comment" class="text-danger"></span>
            </div>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <input type="submit" value="Withdrawal" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Confirmation">Confirm Payment</a>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
